<%= form_for(current_team.active_relationships.find_by(followed_id: @team.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "フォロー解除", class: "btn btn-light-orange" %>
<% end %>