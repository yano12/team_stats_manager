<% if logged_in? %>
  <%= render 'players/home_show' %>
<% else %>
  <header class="masthead text-center text-white">
    <div class="masthead-content">
      <div class="container">
        <h1 class="masthead-heading mb-0">Team App</h1>
        <h2 class="masthead-subheading mb-0">Will Help Your Team  Stats Management</h2>
        <%= link_to "新規登録", team_login_path, class: "btn btn-default btn-xl rounded-pill mt-5" %>
      </div>
    </div>
    <div class="bg-circle-1 bg-circle"></div>
    <div class="bg-circle-2 bg-circle"></div>
    <div class="bg-circle-3 bg-circle"></div>
    <div class="bg-circle-4 bg-circle"></div>
  </header>
<% end %>