<div class="message" data-session="<%= session[:player_id] %>">
  <% player = Player.find_by(id: message.from_id) %>
  <p class="<%= message.id %>" id="message_sender"><%= player.name %></p>
  <p class="<%= message.id %>" id="message_content"><%= message.content %></p>
  <script type="text/javascript">
    var from_id = <%= message.from_id %>;
    var current_player_id = $('.message').data('session');
    if(from_id == current_player_id){
      $('p.' + <%= message.id %>).css('text-align', 'right');
    }else{
      $('p.' + <%= message.id %>).css('text-align', 'left');
    }
  </script>
</div>